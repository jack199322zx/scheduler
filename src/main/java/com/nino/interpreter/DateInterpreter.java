package com.nino.interpreter;

import com.nino.model.ScheduleBean;

import java.util.Date;

public class DateInterpreter implements Interpreter<Date> {

    @Override
    public ScheduleBean interpret(Date origin) {
        return null;
    }
}
